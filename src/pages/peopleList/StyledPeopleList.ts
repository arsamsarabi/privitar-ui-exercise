import styled, { css } from "styled-components";

import { devices } from "../../styles";

const StyledPeopleList = styled.section<WithTheme>(({ theme }) => {
  return css``;
});

export default StyledPeopleList;
